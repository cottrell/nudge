[project]
name = "nudge"
version = "0.1.0"
description = "Composable tmux agent tools with monitor and swarm helpers"
requires-python = ">=3.11"
dependencies = [
  "PyYAML",
]

[dependency-groups]
dev = [
  "pytest",
]
